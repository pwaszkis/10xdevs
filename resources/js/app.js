import './bootstrap';

// Note: Livewire 3 includes and initializes Alpine.js automatically
// We should NOT import Alpine or call Alpine.start() manually
// Instead, we use document.addEventListener('livewire:init') to register Alpine components
